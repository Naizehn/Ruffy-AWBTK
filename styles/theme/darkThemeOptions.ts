import { ThemeOptions } from '@mui/material/styles';

// Crear una instancia para tema modo oscuro.
const darkThemeOptions : ThemeOptions = {
    palette: {
        mode: 'dark',
    },
};

export default darkThemeOptions;