import { ThemeOptions } from '@mui/material/styles';

// Crear una instancia para tema modo claro.
const lightThemeOptions : ThemeOptions = {
    palette: {
        mode: 'light',
    },
};

export default lightThemeOptions;